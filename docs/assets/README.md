# Research figures

`mean_nmi.png` is regenerated from the local mean NMI source matrix using the maintained plotting utility. Titles and axis annotations are in English. The figure shows the matrix with nearest-neighbor raster rendering and a fixed [0, 1] color scale; it does not alter matrix values or recompute the similarity estimator.

Source filename: `lob_similarity_nmi_rellag_lag150_bins2000_mean.csv`.

Source SHA-256: `be7205a608d122854198096a7572b2d661665f042fa4ee83c9f72fe9ddb84393`.

The original two root heatmaps remain available in Git history. This single source-derived figure replaces those duplicate publication assets. The source dates remain unverified, as explained in the [graph provenance](../../data/graphs/README.md).
