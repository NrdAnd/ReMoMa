# Research figures

`mean_nmi.png` visualizes the historical mean NMI matrix with nearest-neighbor rendering and a fixed [0, 1] color scale. It preserves the matrix values without recomputing NMI.

Source filename: `lob_similarity_nmi_rellag_lag150_bins2000_mean.csv`.

Source SHA-256: `be7205a608d122854198096a7572b2d661665f042fa4ee83c9f72fe9ddb84393`.

The source matrix is not distributed. Its dates remain unverified; see [graph provenance](../../data/graphs/README.md). Earlier figures remain available in Git history.
