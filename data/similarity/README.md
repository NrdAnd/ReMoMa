# Similarity matrices

Store generated NMI/similarity matrices here. CSV data in this directory is ignored. Record the estimator, bins, lag range, input dates, source hashes, and training-only fit period before using a matrix to build an evaluation graph.
